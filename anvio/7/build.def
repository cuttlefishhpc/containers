Bootstrap: localimage
From: raw_patched.sif

%labels
    Author "Andrew S. Tupper, PhD"
    Version "1.0"
    Description "Container for running anvio 7"
    Maintainer "Andrew S. Tupper <tuppera@uncw.edu>"
    License "MIT"

%runscript
    if [ $# -eq 0 ]; then
        echo "Usage: anvio@7 <command>"
        echo "Commands:"
        while IFS= read -r command; do
            echo "  $(basename "$command")"
        done < /opt/.commands.txt
    else
        exec "$@"
    fi
